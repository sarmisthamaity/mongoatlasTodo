const express = require('express');
const router = express.Router();
const mergeDataController = require('../controller/mergedata.controller');

// router.get('/:id', mergeDataController.dataFormateOfDatas);

// module.exports = router;